# Path to the SQLite database file (relative to apps/api/)
SQLITE_PATH=./data/library.db

# Max number of rotated log files kept per log (backend.log, frontend.log)
# Each file rotates at 2 MB. With 10 files you get up to ~20 MB of log history per log.
LOG_MAX_FILES=10
